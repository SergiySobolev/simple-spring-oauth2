server:
  port: 9999
  contextPath: /uaa

logging:
  level:
    org.springframework.security: DEBUG

security:
  user:
      password: password
  oauth2:
    client:
      client-id: acme
      client-secret: acmesecret
      scope: read,write
      auto-approve-scopes: '.*'